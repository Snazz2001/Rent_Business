# Local dev / test: leave unset to use the embedded PGlite database automatically.
# Production (Supabase): set to the Supabase Postgres connection string.
DATABASE_URL=
SESSION_SECRET=change-me-in-production
